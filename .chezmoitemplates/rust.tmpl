# vim: set ft=bash:

if ! has rustup; then
    info 'Installing rustup'
    log curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | bash -s -- --no-modify-path -y
    source "$CARGO_HOME/env"
fi
