#!/usr/bin/env bash
# vim: set ft=bash:

source ~/.local/share/chezmoi/.script_helper
set -euo pipefail
trap on_error ERR

# This script is re-executed whenever the nerdfont versions changes.
# Nerdfont: {{ (gitHubLatestRelease "ryanoasis/nerd-fonts").TagName }}
info 'update font cache'
fc-cache -f -v >> "$BOOTSTRAP_LOG_FILE" 2>&1
